{
  "name": "equalizer",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc --build --clean && tsc --build",
    "test": "jest",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/jest": "^29.5.5",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "openai-analyzer": "workspace:^"
  }
}
